// orderModel.js
const pool = require('../config/dbConfig');

const placeOrder = async (userId, products) => {
  
};

const getOrderHistory = async (userId) => {
  //from the database
};

const getOrderById = async (orderId) => {

};


module.exports = {
  placeOrder,
  getOrderHistory,
  getOrderById,
  
};
